(function(){
    angular
        .module('<%= application.name %>')
        .config(configuration);

    function configuration($routeProvider) {
        $routeProvider
        <% include entities/entity-routes -%>
    }
})();